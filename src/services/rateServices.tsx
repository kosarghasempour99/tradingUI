//------------------------------
//---Special Rate
//------------------------------
export const SaveSpecialrates = async (specials:[], pair: string): Promise<void> => {
    // try {
    //     const response = await axios.post(`${BaseUrlApi.baseUrl}/save-special`, {
    //         specials,
    //         pair,
    //     }, {
    //       headers: {
    //         'Content-Type': 'application/json'
    //       }
    //     })
    //     const data = response.data
  
    //     if (response.status != 200 && data.code != 200) {
    //         toast.error(response.data?.message || "Unexpected error occurred...")
    //     }
    // } catch (error) {
    //     toast.error("Save Special rates failed...")
    // }
  }
